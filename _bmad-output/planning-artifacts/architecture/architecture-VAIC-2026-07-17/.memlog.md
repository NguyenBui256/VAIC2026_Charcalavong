---
scope: VAIC enterprise AI-agent platform (full system)
purpose: Build substrate — consistency contract for the build team
altitude: feature (initiative-altitude equivalent for a greenfield platform; keeps epics)
updated: 2026-07-17T15:57
---

- (constraint) MCP server is an external dependency built by a parallel team. Tools in scope there: rag.search, gmail.send, calendar.write, plus the document intake/embedding/indexing infrastructure behind rag.search. VAIC is an MCP CLIENT, not a server author.
- (constraint) FR-2 (Per-Agent KB) splits: VAIC owns document intake UI + department-scoped namespace tagging + per-Agent config; parallel-team MCP server owns chunking/embedding/indexing/retrieval execution. PRD reconciliation needed.
- (constraint) FR-4 (API Integration) — Gmail and Calendar are MCP tools provided by the parallel team, not VAIC-side stubbed integrations. FR-4 may collapse entirely for MVP. PRD reconciliation needed.
- (constraint) PRD §9 'the platform ships with an MCP server component exposing the Task Store and Tool invocation surface' is directly contradicted. VAIC ships an MCP client; the parallel team ships the server. PRD reconciliation needed.
- (constraint) Engineering posture: no over-engineering. Rule of Three before abstraction. Functions <=50 LOC (split before merge if longer). Ship-fast over deep-dive: a thin working slice beats a polished single feature. Defer aggressively.
- (constraint) Definition of Done for every feature: (1) tests pass with evidence (test file:line + green run output), (2) code reference (production file:line). No feature is 'done' until both are visible in the PR/close-out.
- (decision) Paradigm: Hexagonal Modular Monolith. One FastAPI process, bounded modules aligned to PRD feature groups, hexagonal (ports & adapters) inside each. Chosen over microservices (2-day timeline, single team) and pure layered (Model Layer / Tools / KB / MCP are inherently ports-and-adapters shapes, layered doesn't name them).
- (version) Stack pinned (web-verified 2026-07-17): Python 3.12, FastAPI 0.139.x, PostgreSQL 16, pgvector 0.7+ (only if VAIC-side retrieval is needed — pending FR-2 reconciliation), arq 0.26+ with cron_jobs (single async-job mechanism; replaces Celery+Beat and APScheduler), Redis 7 (arq broker only, not for run state), Pydantic 2.x, SQLAlchemy 2.x, Anthropic SDK 0.114.0, OpenAI SDK latest, MCP Python SDK v1.x (v2.0.0 due 2026-07-28 — stick to v1 for the hackathon), React 19, Vite 7, TypeScript 5.6+, Tailwind 4.
- (assumption) OQ-4 (deployment target): single laptop, docker-compose with Postgres + Redis + FastAPI app + React static-served. Demoable locally. Not cloud for the hackathon.
- (assumption) OQ-2 (LLM providers): Anthropic Claude + OpenAI GPT configured in Model Layer. Team supplies both API keys. Per-Agent model selection (FR-5) exercised by configuring demo Agents on different providers.
- (decision) OQ-6 (Tool sandbox): subprocess + no-network + restricted builtins + 10s CPU + 128MB memory cap. MVP-only — flagged as known downgrade. Production needs WASM (wasmtime-py) or E2B. Most demo Tools route via MCP (rag.search, gmail.send, calendar.write) so the embedded-Python sandbox only matters for any VAIC-side Tool the team chooses to write.
- (decision) OQ-10 (run state): Postgres-only. workflow_runs table is the source of truth; arq carries the work; Redis is broker-only. No Redis-cached run state for MVP — keeps resume semantics single-source.
- (decision) MCP Task Store question (PRD FR-9 'MCP also serves as shared Task Store'): relaxed. VAIC persists Task state in its own Postgres tasks table. Orchestrator-to-Agent dispatch is an internal domain operation, NOT over MCP. MCP is strictly for invoking external tools on the parallel-team server. PRD reconciliation needed.
- (event) Reviewer Gate run — 2 reviewers in parallel (stack-verification + adversarial-divergence). Both PASS-WITH-WARNINGS. Stack: 5 version pins stale (TS 5.6->7.x, Vite 7->8, Python 3.12->3.13, PG 16->18, Redis '7'->7.4+). Adversarial: 5 holes (2 CRITICAL, 3 HIGH) — all closed via tightened AD-6/AD-8 + new AD-10/AD-11 + AD-4 failure-mode rule + Convention rows for MCP scope and background-job tenant context.
- (decision) AD-10 added: Tenant context materialized in arq job payloads, re-set at worker entry. Schedule Triggers fan out per-tenant from a single cron_jobs entrypoint. Closes the classic contextvar-doesn't-cross-process footgun the adversarial reviewer flagged CRITICAL.
- (decision) AD-11 added: Client-side department scope enforcement on every McpClientPort call. The only mechanism standing behind FR-2 department isolation while retrieval is outsourced to the parallel-team MCP server.
- (decision) AD-6 tightened: compare-and-set on ALL state machines (workflow_runs, tasks, mini_app_rows.version), with rowcount==1 check. AD-8 tightened: Provisioner creates namespace + initial rows atomically; auto-gen CRUD endpoints are sole writers post-provisioning. AD-4 tightened: audit.log() failure crashes the Run (honors SM-C1 trace-completeness counter-metric).
- (event) Spine finalized. status: final. 11 ADs (5 ADOPTED-from-PRD, 6 new/tightened). 6 open questions, 4 PRD reconciliation items deferred to next bmad-edit-prd pass. Ready for downstream: bmad-create-epics-and-stories next, or bmad-spec if a spec companion is wanted.
