/* Story 2.2 — Prompt tab placeholder. Functionality arrives in Story 2.3. */

import { Card } from "../../ui";

export default function PromptTab() {
  return (
    <Card title="Prompt">
      <p className="text-body" style={{ color: "var(--color-text-tertiary)" }}>
        Coming soon — the prompt editor arrives in Story 2.3.
      </p>
    </Card>
  );
}
